# CHANGELOG

## Unreleased

+ [调整] 项目名称与技术标识统一更新为“影格工坊 / FrameForge / frameforge”，覆盖 README、文档站、浏览器标题、部署说明、Codex 插件、MCP、导出标识、本地存储 key 和项目目录。
+ [调整] 全站助手从 Codex app-server 切换为 Hermes ACP 连接，由本地 Canvas Agent 启动 `hermes-acp` 并注入画布 MCP 工具。
+ [修复] 整理角色设定统一改为先选择要发送的剧本章节，避免全文输入导致模型续写剧本或返回非 JSON。
+ [优化] 超长剧本改为章节分页阅读与防抖自动保存，避免全量 Markdown 反复渲染导致画布卡顿。
+ [新增] 生成配置节点右侧新增输出连线点，可连接到用户上传的图片、视频或音频资产节点。
+ [新增] 图片节点工具栏新增“复制图片”，可将图片复制到浏览器系统剪贴板。
+ [修复] 剧本全屏阅读器按 `Esc` 可稳定退出，避免焦点在阅读器内部时无法关闭。
+ [新增] 新增《月光樱花门》全季小说与全季剧本 Markdown 文件，覆盖 12 集樱花节月光奇幻短剧。
+ [调整] 主题驱动 3D 动物短剧 skill 的角色素材重建为 `人物角色` 目录下的 5 张组图，并改用组图位置锁定角色身份。
+ [调整] 主题驱动 3D 动物短剧 skill 的即梦 Prompt 改为默认 8-10 秒生成段落，可覆盖多个连续分镜，避免 3-5 秒小镜头过短。
+ [优化] 主题驱动 3D 动物短剧 skill 默认加入超清细节、主体锐利、4K、sharp focus 等固定画质关键词，并要求主体清晰对焦、避免运动模糊。
+ [优化] 主题驱动 3D 动物短剧 skill 增加角色朝向与镜头站位规则，避免角色看向目标物时误生成面对镜头。
+ [优化] 主题驱动 3D 动物短剧 skill 将牌子、信件、书页等可读文字改为视频软件文字层交付，避免即梦直接生成乱码文字。
+ [优化] 主题驱动 3D 动物短剧 skill 的生成段落 Prompt 正文统一以“生成视频：X 秒，”开头，方便直接复制到即梦。
+ [优化] 主题驱动 3D 动物短剧 skill 的角色称呼改为具体动物名加角色名，提升即梦全能参考的人物识别稳定性。
+ [优化] 主题驱动 3D 动物短剧 skill 改为默认面向即梦全能参考模式，按场景参考、人物参考和生成段落简介拆分视频 Prompt，不再按首尾帧方案交付。
+ [调整] 画布、素材、生成记录、配置与原始媒体改由本机存储服务直接保存到 `data/`，重启或更换浏览器后可从磁盘恢复。
+ [优化] 镜头无真实阻塞时自动选择生成方式并带入已有视觉参考，首帧等画面上传后直接采用，主流程不再要求确认镜头、确认方案或审核参考图。
+ [修复] 同名角色重复时优先使用已上传视觉参考的角色记录，打开镜头工作台会立即重新匹配并清除遗留的“缺少人物”异常。
+ [修复] 已存在的角色设定会按镜头文本或旧异常中的同名角色重新匹配旧分镜并关闭“缺少人物”异常，恢复画布或上传角色图时会自动接入已确认镜头的视觉参考。
+ [修复] 上传角色、场景或道具参考图后会自动接入已确认镜头，分镜与镜头工作台可立即显示视觉参考数量。
+ [优化] 镜头流程收敛为检查分镜、准备画面、外部生成、回传结果四步，默认隐藏内部版本、修订和任务档案信息。
+ [修复] “身份、角色设定、镜头、动作”等语义词不再被识别为缺少人物，旧画布刷新后会自动清理此类误报。
+ [修复] 镜头资产识别不再将“手持玉佩”等动作加道具描述误判为人物，并可将“玉佩”匹配到“旧玉佩”等带修饰词的正式道具。
+ [修复] 资产卡草案兼容平铺或嵌套资产字段及遗漏比例的模型返回，仍按当前资产类型严格校验创作字段。
+ [修复] 资产卡草案兼容模型将单个草案包装为数组的返回格式，多个无法唯一匹配的草案会明确提示处理。
+ [新增] 人物、场景和道具卡支持回填外部生成图为版本化标准参考图，并可在镜头控制资产中复用。
+ [优化] 缺失资产与正式资产卡改用高质量动画 3D 生产级 Prompt 编译，外部工具可获得完整正负 Prompt、比例和制作流程。
+ [优化] 资产卡草案生成支持等待计时、手动停止、90 秒超时和刷新中断恢复，避免任务长期停在生成中。
+ [修复] 缺失资产卡生成会显示可见的生成中、草案就绪或失败原因，不再出现点击无反馈。
+ [修复] AI 镜头整理兼容资产状态中附带的冗余资产描述字段，仍保留资产 ID、版本和镜头归属校验。
+ [优化] 分镜生产看板将“重新检查整集”改为可见的 AI 检查任务，展示检查范围、进行状态和完成结果。
+ [修复] 草稿镜头在无阻塞异常时可一键确认镜头并确认生成方案，旧剧本重新整理时会识别主要人物缺失而非误判为空镜。
+ [新增] 结构化分镜生成后会自动执行整集 AI 整理，高置信度镜头自动确认，工作台默认只展示可读摘要、资产匹配和异常处理。
+ [新增] 缺失人物、场景或道具支持生成结构化资产卡草案，采用后自动绑定相关镜头，并可一键生成参考图或复制 Prompt。
+ [修复] 镜头工作台的人物绑定会自动同步画布人物卡为版本化资产，旧画布和未连接分镜配置的人物卡也可直接选择。
+ [修复] 结构化分镜可修复模型返回的常见 JSON 语法和字段名损坏，并在本地修复失败时自动执行一次受约束模型修复。
+ [修复] 结构化分镜兼容模型返回空连续性约束，并按镜头原有节奏将总时长归一到目标时长。
+ [修复] 结构化分镜生成会按导演要求截取目标单集，并兼容模型遗漏来源说明、扩展镜头分类和常见转场别名。
+ [修复] 修复生成配置选择“结构化分镜”后底部“开始生成”按钮被节点高度裁掉的问题。
+ [修复] 本地访问 `127.0.0.1` 时自动切换到 `localhost`，避免因浏览器来源隔离误显示为空画布库。
+ [新增] 分镜表支持整集生产看板、即梦候选验收、跨镜连续性检查，并可导出剪映执行单与包含实际媒体的标准生产包。
+ [新增] 镜头工作台支持版本化控制资产和即梦任务单，可输出模式设置、上传顺序、引用映射、最终提示词、验收与重试流程。
+ [新增] 镜头节点支持全屏工作台，可编辑镜头、绑定版本化资产、检查阻塞项并确认首帧、首尾帧、智能多帧或全能参考方案。
+ [新增] 生成配置支持从剧本与人物、场景、道具目录生成结构化分镜，并创建可折叠的分镜表和镜头节点。
+ [调整] 空白画布左键拖动改为框选节点，画布平移改用空格加左键、鼠标中键或触控板双指滑动。
+ [新增] 画布工具栏新增一键整理，支持选中范围或全画布从左到右自动布局并适配视口。
+ [新增] 人物卡支持通过模型生成或外部制作两种方式创建角色视觉资产包，并导入三视图、表情九宫格和景别参考图。
+ [新增] 剧本节点新增批量生成人物卡能力，支持创建可折叠的人物卡组和主要角色人物卡。
+ [新增] 画布新增 Markdown 文档节点，配置节点文本生成可选择输出为 Markdown 文档并清洗模型思考标签。
+ [新增] 画布新增 Markdown 剧本节点，支持双击进入全屏阅读和编辑剧本。
+ [新增] 配置页新增 MiniMax 生图和文本渠道快捷入口及对应调用脚本模板。
+ [修复] 修复人物卡组展开后人物卡和连线未显示的问题，并对齐批量图片的展开收起动画。
+ [调整] 移除首页，默认访问根路径时直接进入我的画布。
+ [调整] 移除顶部品牌 Logo 入口并将画布列表页标题改为画布库。
+ [调整] 移除右上角文档和 GitHub 外链入口。
+ [修复] 修复配置节点切到文本生成时仍按生图张数生成多个文本结果的问题。
+ [修复] 修复全站助手连接状态可能触发 React 最大更新深度错误的问题，并补充页面异常兜底。
+ [修复] 画布触控板双指滑动改为平移画布，并提高双指捏合缩放灵敏度。
